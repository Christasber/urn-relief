import { NextRequest, NextResponse } from 'next/server'
export async function POST(req: NextRequest) {
  // This stub would generate a quick GLB preview server-side if needed.
  return NextResponse.json({ ok: true })
}
